import { StackScreenProps } from '@react-navigation/stack';
import { MainStackParamList } from 'navigation/Navigation.types';

export interface DetailPromoProps extends StackScreenProps<MainStackParamList, 'Detail/Promo'>{
}
